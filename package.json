{
  "name": "code-se19",
  "version": "1.0.0",
  "description": "This project is aimed to show all the successes that happened in the Young Founders Network. The goal is to have a simple input UI so that everybody can add successes and then show it in a simple format. Ideally it would be readable in an rss format, so that it can be implemented in other Blogs",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js",
    "format": "npx prettier . --write",
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.9",
    "express": "^4.18.3",
    "mongoose": "^8.2.3",
    "prettier": "^3.2.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4"
  }
}
